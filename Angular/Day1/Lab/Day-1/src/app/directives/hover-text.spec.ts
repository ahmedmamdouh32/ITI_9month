import { HoverText } from './hover-text';

describe('HoverText', () => {
  it('should create an instance', () => {
    const directive = new HoverText();
    expect(directive).toBeTruthy();
  });
});
