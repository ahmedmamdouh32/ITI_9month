import { DescriptionFormatPipe } from './description-format-pipe';

describe('DescriptionFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
